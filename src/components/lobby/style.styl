@import '~jeet/jeet.styl'
@import '../../utils/mixins.styl'

imagePath = '../../../images/';

.erudite-battles-lobby-component
    column(1/3, 1/3)
    display: flex
    flex-direction: column
    justify-content: center
    align-items: center
    height: 100%;

    .find-match-button
        button(imagePath)
